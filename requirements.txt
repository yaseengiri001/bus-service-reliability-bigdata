# ── Big Data Programming Project — Python dependencies ─────────────────
# Core distributed engine (mandatory for the pipeline)
pyspark==3.5.3

# Memory-scale preprocessing / plotting bridges (justified per-stage in report)
pandas>=2.0.0
numpy>=1.24.0
pyarrow>=14.0.0          # Parquet I/O + Spark<->Pandas Arrow transfer

# Machine learning support / metrics helpers
scikit-learn>=1.3.0

# Exploratory data analysis & visualisation
matplotlib>=3.7.0
seaborn>=0.12.0
plotly>=5.18.0

# Interactive dashboard (localhost)
streamlit>=1.30.0

# ── Report generation tooling ─────────────────────────────────────────
scipy>=1.11.0           # Q-Q plot / residual statistics
markdown>=3.5           # report Markdown -> HTML
pygments>=2.17          # syntax-highlighted code screenshots
playwright>=1.40        # dashboard screenshots (channel="chrome", no browser download)
weasyprint>=60          # HTML -> PDF (macOS native deps: brew install pango gdk-pixbuf libffi)
